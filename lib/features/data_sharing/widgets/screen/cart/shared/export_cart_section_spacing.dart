import 'package:flutter/widgets.dart';

class ExportCartSectionSpacing
    extends StatelessWidget {
  const ExportCartSectionSpacing({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 20);
  }
}